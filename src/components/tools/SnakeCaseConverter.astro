---
---
<div class="space-y-4">
  <textarea id="skc-input" rows="7" placeholder="Hello World Example&#10;myVariableName&#10;kebab-case-input" class="w-full bg-[var(--bg-surface)] border border-slate-700 rounded-xl px-4 py-3 text-slate-100 placeholder-slate-500 resize-y focus:outline-none focus:border-violet-500 text-sm font-mono"></textarea>
  <div class="flex gap-2">
    <button id="skc-convert" class="px-5 py-2 bg-violet-600 hover:bg-violet-500 text-white rounded-lg text-sm font-medium transition-colors">Convert to snake_case</button>
    <button id="skc-clear" class="px-4 py-2 bg-[var(--bg-surface)] hover:bg-[var(--bg-surface-hover)] text-slate-400 rounded-lg text-sm transition-colors border border-slate-700">Clear</button>
  </div>
  <div id="skc-output-wrap" class="hidden space-y-2">
    <div class="flex items-center justify-between">
      <span class="text-xs text-slate-500">snake_case result</span>
      <button id="skc-copy" class="text-xs text-violet-400 hover:text-violet-300 transition-colors">Copy</button>
    </div>
    <textarea id="skc-output" rows="7" readonly class="w-full bg-[var(--bg-surface)] border border-slate-700 rounded-xl px-4 py-3 text-slate-100 resize-y text-sm font-mono select-all"></textarea>
  </div>
</div>
<script>
  function toSnake(str) {
    return str
      .replace(/([a-z])([A-Z])/g, '$1_$2')
      .replace(/([A-Z]+)([A-Z][a-z])/g, '$1_$2')
      .replace(/[\s\-]+/g, '_')
      .replace(/[^a-zA-Z0-9_]/g, '')
      .replace(/_+/g, '_')
      .replace(/^_|_$/g, '')
      .toLowerCase();
  }
  const input = document.getElementById('skc-input');
  const output = document.getElementById('skc-output');
  const wrap = document.getElementById('skc-output-wrap');
  const saved = localStorage.getItem('tw-snake-case-converter');
  if (saved) input.value = saved;
  input.addEventListener('input', () => localStorage.setItem('tw-snake-case-converter', input.value));
  document.getElementById('skc-convert').addEventListener('click', () => {
    output.value = input.value.split('\n').map(l => l.trim() ? toSnake(l) : '').join('\n');
    wrap.classList.remove('hidden');
  });
  document.getElementById('skc-clear').addEventListener('click', () => { input.value = ''; output.value = ''; wrap.classList.add('hidden'); localStorage.removeItem('tw-snake-case-converter'); });
  document.getElementById('skc-copy').addEventListener('click', () => navigator.clipboard.writeText(output.value));
</script>
